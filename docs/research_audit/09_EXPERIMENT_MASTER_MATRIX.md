# 09 — Experiment Master Matrix

All experiments across Stages 0–6. **No results are pre-written.** Every row is a hypothesis to test, with pre-registered success/failure thresholds and next actions.

## Compute Class Legend

| Class | Hardware | Typical Duration | GPU-Hours |
|-------|----------|-----------------|-----------|
| CPU | CPU only | < 5 min | 0 |
| GPU-short | RTX 5080 16GB | < 30 min | < 0.5 |
| GPU-medium | RTX 5080 16GB | 2–6 h | 2–6 |
| GPU-full | RTX 5080 16GB | 8–48 h | 8–48 |

## Stage 0: Static Validation

**Compute class**: CPU, XS effort. **Purpose**: Verify code correctness without data or training.

| ID | Research Question | Hypothesis (H1) / Null (H0) | Models | Dataset / Split | Seeds | Eval Samples | Sampler | Primary Metric | Secondary Metric | Controls | Compute | Success Threshold | Failure Threshold | Next Action |
|----|-------------------|------------------------------|--------|-----------------|-------|-------------|---------|----------------|-----------------|----------|---------|-------------------|-------------------|-------------|
| E0-01 | Does graph construction detect cycles? | H1: Detects all cycles. H0: Misses cycles. | N/A | N/A | N/A | N/A | N/A | cycle_detection_accuracy | false_positive_rate | 100 random DAGs + 100 cyclic graphs (generated by NetworkX) | CPU | TP=1.0, FP=0.0 | Any missed cycle or false positive | Fix validation, re-run |
| E0-02 | Does topological sort validate DAG edge ordering? | H1: TSort catches all violations. H0: Some violations pass. | N/A | N/A | N/A | N/A | N/A | topological_violation_detection | N/A | Random DAGs with injected edge reversals (note: node numbering ≠ topological order in sync updates) | CPU | 100% detection | Any missed violation | Fix validation, re-run |
| E0-03 | Does InterventionSpec validate inconsistent modes? | H1: All invalid combos raise ValueError. H0: Some invalid combos pass. | N/A | N/A | N/A | N/A | N/A | invalid_spec_rejection_rate | false_negative on valid specs | Table of valid + invalid spec combinations | CPU | 100% rejection of invalid | Any invalid spec accepted silently | Fix validation, re-run |
| E0-04 | Does config roundtrip (dict → model → dict) preserve all fields? | H1: Full preservation. H0: Fields lost. | FGRDiT (all graph modes) | N/A | N/A | N/A | N/A | field_match_fraction | diff_report | All graph modes (INDEPENDENT, DAG, DENSE_DIRECTED, CUSTOM) | CPU | 1.0 match | Any field divergence | Fix serialization |
| E0-05 | Does checkpoint roundtrip produce identical output? | H1: Bit-exact match. H0: Output differs. | FGRDiT (random init) | N/A | N/A | N/A | N/A | max_abs_diff | N/A | Random input, save→load→forward, compare | CPU | max_diff < 1e-6 | max_diff ≥ 1e-6 | Non-deterministic layer — investigate |

## Stage 1: Sampler Identity Tests

**Compute class**: CPU, S effort. **Purpose**: Verify deterministic generation and NoiseTrace coupling before any training.

| ID | Research Question | Hypothesis (H1) / Null (H0) | Models | Dataset / Split | Seeds | Eval Samples | Sampler | Primary Metric | Secondary Metric | Controls | Compute | Success Threshold | Failure Threshold | Next Action |
|----|-------------------|------------------------------|--------|-----------------|-------|-------------|---------|----------------|-----------------|----------|---------|-------------------|-------------------|-------------|
| E1-01 | Does deterministic DDIM (η=0) produce bit-exact output on repeated calls? | H1: Bit-exact. H0: Output varies. | FGRDiT (random init) | Fixed batch of 32 random x_T | N/A | 32 | DDIM η=0 | max_abs_diff | N/A | Same x_T, same condition, 2 calls | CPU | max_diff == 0.0 | max_diff > 0 | Fix non-determinism (cudnn, atomic ops) |
| E1-02 | Does stochastic DDPM with shared NoiseTrace produce identical output? | H1: Bit-exact when seeds match. H0: Output varies. | FGRDiT (random init) | Fixed batch of 32 random x_T | N/A | 32 | DDPM (counter seeds) | max_abs_diff | N/A | Same trace (same x_T + same seed grid), 2 calls | CPU | max_diff == 0.0 | max_diff > 0 | Fix counter-based RNG integration |
| E1-03 | Does deterministic DDIM diverge from stochastic DDPM on same x_T? | H1: Outputs differ (different sampling algorithms). H0: Outputs identical (bug). | FGRDiT (random init) | Fixed batch of 32 random x_T | N/A | 32 | DDIM vs DDPM (same x_T) | mean_L2_distance | LPIPS | Same x_T, different samplers | CPU | mean_L2 > 0.01 | mean_L2 ≈ 0 | Samplers producing same trajectory — check implementation |
| E1-04 | Does NoiseTrace storage (seeds) produce same output as full-noise storage? | H1: Bit-exact match. H0: Output differs. | FGRDiT (random init) | Fixed batch of 32 | N/A | 32 | DDPM | max_abs_diff | N/A | Same x_T, seeds-based vs tensor-based noise | CPU | max_diff == 0.0 | max_diff > 0 | Seed→noise mapping inconsistent |
| E1-05 | Does Path Non-Interference Theorem hold at the denoiser level? | H1: ∂ε/∂e_i = 0 under FACTOR_SOURCE_CUT. H0: Non-zero gradient. | FGRDiT (random init) | Fixed batch of 32 random (x_t, t, f) | N/A | 32 | N/A (denoiser only) | max_abs_diff(ε(f_i), ε(f_i')) | grad_norm w.r.t e_i | FACTOR_SOURCE_CUT mode, compare ε for f_i vs f_i' | CPU | max_diff < 1e-5 | max_diff ≥ 1e-5 | Architecture has bypass path — critical defect |

## Stage 2: Deterministic Micro-Overfit

**Compute class**: GPU-short, S effort. **Purpose**: Verify model can memorize, factor source cut is exact on trained model, neural_graph_surgery works.

| ID | Research Question | Hypothesis (H1) / Null (H0) | Models | Dataset / Split | Seeds | Eval Samples | Sampler | Primary Metric | Secondary Metric | Controls | Compute | Success Threshold | Failure Threshold | Next Action |
|----|-------------------|------------------------------|--------|-----------------|-------|-------------|---------|----------------|-----------------|----------|---------|-------------------|-------------------|-------------|
| E2-01 | Can FGR overfit 16 fixed (x_t, t, noise_target, condition) tuples? | H1: Loss → 0. H0: Loss plateaus above 0. | FGRDiT (K=3, INDEPENDENT) | 16 pre-generated tuples | 1 | 16 | N/A (denoiser only) | MSE (ε_pred, ε_true) | Per-stream output norm | Fixed batch, repeated training | GPU-short | MSE < 1e-4 within 5000 steps | MSE > 0.01 after 5000 steps | Model capacity bug — check parameter count and architecture |
| E2-02 | Is path cut invariance exact on overfit model? | H1: Bit-exact after cut. H0: Small residual difference. | FGRDiT (overfit from E2-01) | Same 16 tuples | 1 | 16 | N/A (denoiser) | max_abs_diff (cut vs changed-factor) | N/A | FACTOR_SOURCE_CUT mode, f_i vs f_i' | GPU-short | max_diff < 1e-5 | max_diff ≥ 1e-5 | Cross-stream information leakage via shared trunk or base stream |
| E2-03 | Does neural_graph_surgery mode produce non-zero edit effect? | H1: Neural-graph-surgically edited output differs from original. H0: No effect. | FGRDiT (overfit from E2-01) | Same 16 tuples | 1 | 16 | N/A (denoiser) | mean_L2(ε_surgery, ε_original) | Per-stream contribution | neural_graph_surgery on one node, compute ε | GPU-short | mean_L2 > 0.01 | mean_L2 ≈ 0 | Surgery mode not wired correctly |
| E2-04 | Does IndependentStreamDiT also overfit (baseline parity)? | H1: Overfits at similar rate. H0: Cannot overfit. | IndependentStreamDiT | Same 16 tuples | 1 | 16 | N/A (denoiser) | MSE | Steps to convergence | Same setup as E2-01 | GPU-short | MSE < 1e-4 | MSE > 0.1 at convergence | Baseline architecture broken |

## Stage 3: Stochastic Micro-Overfit

**Compute class**: GPU-short, M effort. **Purpose**: Verify training loop stability with random t/noise, test on real data minibatches.

| ID | Research Question | Hypothesis (H1) / Null (H0) | Models | Dataset / Split | Seeds | Eval Samples | Sampler | Primary Metric | Secondary Metric | Controls | Compute | Success Threshold | Failure Threshold | Next Action |
|----|-------------------|------------------------------|--------|-----------------|-------|-------------|---------|----------------|-----------------|----------|---------|-------------------|-------------------|-------------|
| E3-01 | Does FGR training loop run without NaN on dSprites minibatch? | H1: Stable training. H0: NaN within 100 steps. | FGRDiT (K=3) | dSprites, 640 samples (1% of train) | 1 | N/A | N/A | train_loss_curve | grad_norm_trace | Float64 sanity check, gradient clipping | GPU-short | Loss decreasing, no NaN in 500 steps | NaN in first 100 steps | Numerical instability — check embedding, attention, adaLN |
| E3-02 | Does base stream + per-stream projection train without divergence? | H1: Stable. H0: Diverges. | FGRDiT (full: base stream, per-stream Norm_i+P_i) | dSprites, 640 samples | 1 | N/A | N/A | train_loss_curve | per_stream_output_norms | Compare with/without base stream | GPU-short | Loss decreasing, no NaN in 500 steps | Divergence or NaN | Base stream or projection initialization wrong |
| E3-03 | Does stochastic micro-overfit reach low loss (32 images, random t/noise)? | H1: MSE < 0.001. H0: Loss plateaus. | FGRDiT | dSprites, 32 real images, random t/noise per step | 1 | N/A | N/A | MSE at step 2000 | N/A | Loss on held-out 8 images | GPU-short | Train MSE < 0.001, Val MSE < 0.01 | Train MSE > 0.01 at step 2000 | Optimizer/scheduler not effective |

## Stage 4: dSprites Pilot

**Compute class**: GPU-medium, L effort. **Purpose**: First real evaluation — does FGR show factor-editing advantage on simple synthetic data?

| ID | Research Question | Hypothesis (H1) / Null (H0) | Models | Dataset / Split | Seeds | Eval Samples | Sampler | Primary Metric | Secondary Metric | Controls | Compute | Success Threshold | Failure Threshold | Next Action |
|----|-------------------|------------------------------|--------|-----------------|-------|-------------|---------|----------------|-----------------|----------|---------|-------------------|-------------------|-------------|
| E4-01 | Does FGR reduce off-target leakage vs canonical DiT on factor_edit? | H1: L_ij(FGR) < L_ij(DiT) for i≠j. H0: No difference. | FGRDiT (INDEPENDENT), DiT (shared trunk) | dSprites, S0 IID | 2 | 1000 | DDIM η=0 | off_target_mean (factor_edit protocol) | leakage_matrix, target_accuracy | NC1-NC5, oracle accuracy baseline | GPU-medium | FGR off-target < DiT off-target (direction-only, pilot signal) | FGR ≥ DiT | FGR path isolation not empirically meaningful at this scale |
| E4-02 | Does FGR target success equal or exceed DiT on factor_edit? | H1: target_accuracy(FGR) ≥ target_accuracy(DiT). H0: FGR worse. | FGRDiT (INDEPENDENT), DiT | dSprites, S0 IID | 2 | 1000 | DDIM η=0 | target_accuracy per factor | LPIPS(orig, edit) | NC1, NC3 | GPU-medium | FGR ≥ DiT (direction-only) | FGR < DiT | Factor paths hurt editing capability |
| E4-03 | Does FGR outperform IndependentStreamDiT on factor_edit? | H1: FGR better due to ROST-FRG architecture with independent adapters. H0: IndependentStreamDiT equal or better. | FGRDiT (INDEPENDENT), IndependentStreamDiT | dSprites, S0 IID | 2 | 1000 | DDIM η=0 | off_target_mean | target_accuracy, leakage_matrix | NC1-NC5 | GPU-medium | FGR off-target < IndStream off-target (direction-only) | FGR ≥ IndStream | Independent adapter branches provide no benefit over fully independent streams |
| E4-04 | Does held-out pair split (S1) show generalization vs IID split? | H1: Held-out harder for all models, FGR degrades less. H0: No difference. | FGRDiT, DiT, IndependentStreamDiT | dSprites, S0 vs S1 | 2 | 1000 | DDIM η=0 | target_accuracy (S1) / target_accuracy (S0) | leakage_matrix | Oracle accuracy on held-out pairs | GPU-medium | FGR ratio > 0.85, FGR ratio > DiT ratio | FGR ratio < 0.7 | Models memorize factor-value combinations, no generalization |
| E4-05 | Does factor source cut invariance hold on trained model (Protocol 2)? | H1: max_diff < 1e-3 on trajectory. H0: Non-zero difference. | FGRDiT (trained) | dSprites, S0 | 2 | 1000 | DDIM η=0 | max_abs_diff (cut vs changed-factor trajectory) | per_timestep_diff_curve | FACTOR_SOURCE_CUT mode, f_i vs f_i' | GPU-medium | max_diff < 1e-3 | max_diff ≥ 1e-3 | Path Non-Interference fails at trajectory level — possible in practice, adjust claim scope |
| E4-06 | Does gate training (exposure during training) improve over inference-time gating only? | H1: Trained gates give better factor_edit performance. H0: No difference. | FGRDiT (gate_train=True vs False) | dSprites, S0 | 2 | 1000 | DDIM η=0 | target_accuracy, off_target_mean | gate_entropy, gate_sparsity | NC1-NC5 | GPU-medium | Gate-trained > gate-untrained on ≥1 metric (direction-only) | Gate-trained ≤ untrained | Gate training doesn't help — drop from method |

## Stage 5: 3DShapes + Graph Correctness

**Compute class**: GPU-medium, L effort. **Purpose**: Scale to 6 factors, test graph misspecification on Causal3DIdent.

| ID | Research Question | Hypothesis (H1) / Null (H0) | Models | Dataset / Split | Seeds | Eval Samples | Sampler | Primary Metric | Secondary Metric | Controls | Compute | Success Threshold | Failure Threshold | Next Action |
|----|-------------------|------------------------------|--------|-----------------|-------|-------------|---------|----------------|-----------------|----------|---------|-------------------|-------------------|-------------|
| E5-01 | Does FGR with 6 factor streams train stably on 3DShapes? | H1: Stable training, loss converges. H0: NaN or divergence. | FGRDiT (K=6, INDEPENDENT) | 3DShapes, S0 | 2 | 1000 | DDIM η=0 | train_loss_curve | per_stream_output_norms | Float64 grad check | GPU-medium | Loss decreasing, no NaN | NaN within 10K steps | O(K) cost scaling issue — revisit architecture |
| E5-02 | Does FGR maintain editing advantage at K=6? | H1: FGR < DiT on off-target leakage. H0: No advantage. | FGRDiT (INDEPENDENT), DiT, IndependentStreamDiT | 3DShapes, S0 | 3 | 1000 | DDIM η=0 | off_target_mean | leakage_matrix (6×6) | NC1-NC5 | GPU-medium | FGR off-target < DiT (direction-only per factor) | FGR ≥ DiT on all factors | FGR doesn't scale past K=3 |
| E5-03 | Does correct graph (Causal3DIdent) produce lower leakage than empty graph? | H1: correct_DAG_leakage < empty_DAG_leakage. H0: No difference. | FGRDiT (correct vs empty DAG) | Causal3DIdent, S0 | 3 | 1000 | DDIM η=0 | off_target_mean | leakage_matrix, per-pair leakage | Factorial: correct, empty, DENSE_DIRECTED, reversed, random | GPU-medium | correct < empty (direction-only with effect size d>0.2) | correct ≥ empty | Graph structure doesn't matter — pivot to auditability-only paper (Pivot A) |
| E5-04 | Do all wrong DAGs perform worse than correct DAG? | H1: correct < all_wrong on leakage. H0: Some wrong DAGs equal correct. | FGRDiT (5 graph variants) | Causal3DIdent, S0 | 3 | 1000 | DDIM η=0 | ranking: leakage(correct) < leakage(empty) < leakage(DENSE_DIRECTED, reversed) | per-edge attention weight statistics | NC4 (graph misspecification controls) | GPU-medium | correct ranked best, DENSE_DIRECTED/reversed ranked worst (direction-only) | Wrong DAG equal or better than correct | Kill criterion: graph alignment doesn't matter |
| E5-05 | Does neural_graph_surgery (Protocol 3) show lower non-descendant leakage than factor_edit? | H1: neural_graph_surgery < factor_edit on non-descendants. H0: No difference. | FGRDiT (correct graph) | Causal3DIdent, S0 | 3 | 1000 | DDIM η=0 | non_descendant_leakage_mean (Protocol 3 vs Protocol 1) | descendant_change (should be nonzero) | node_deletion, edge_ablation as baselines | GPU-medium | surgery < edit (direction-only) | surgery ≥ edit | Incoming edge cut doesn't isolate node — graph structure not meaningful at neuron level |

## Stage 6: Confirmatory

**Compute class**: GPU-full, XL effort. **Purpose**: Full evaluation with all baselines, 5 seeds, all splits, all protocols. Final results for paper.

| ID | Research Question | Hypothesis (H1) / Null (H0) | Models | Dataset / Split | Seeds | Eval Samples | Sampler | Primary Metric | Secondary Metric | Controls | Compute | Success Threshold | Failure Threshold | Next Action |
|----|-------------------|------------------------------|--------|-----------------|-------|-------------|---------|----------------|-----------------|----------|---------|-------------------|-------------------|-------------|
| E6-01 | Full baseline comparison: FGR vs DiT vs IndependentStreamDiT vs CrossAttnDiT vs CF-DiT on all metrics | H1: FGR best on leakage, competitive on quality. H0: FGR not distinguishable. | FGRDiT, canonical DiT, IndependentStreamDiT, CrossAttnDiT, CF-DiT | dSprites (S0, S1, S2, S3), 3DShapes (S0, S1), Causal3DIdent (S0) | 5 | 1000 | DDIM η=0 | leakage matrix, target accuracy, FID | LPIPS, gate AUC, per-stream contribution norms | All NC1-NC5, oracle calibration | GPU-full | FGR best on off-target leakage across datasets, CI-corrected | FGR not better on leakage | Paper contribution shifts to null result or architecture comparison only |
| E6-02 | Held-out triple (S2) and systematic compositional (S3) generalization | H1: FGR degrades < baselines on compositional splits. H0: All models degrade equally. | All 5 models | dSprites, S2+S3 | 5 | 1000 | DDIM η=0 | target_accuracy(Sx)/target_accuracy(S0) | leakage_matrix, oracle accuracy | Factor-value histogram (train vs test coverage) | GPU-full | FGR ratio > 0.7, FGR > DiT ratio | FGR ratio < 0.5 | Compositional generalization not helped by factor paths |
| E6-03 | Base stream contribution analysis | H1: Base stream absorbs nuisance (position x/y) and contributes < 50% of output norm. H0: Base stream dominates. | FGRDiT (with base stream) | dSprites, S0 | 5 | 1000 | DDIM η=0 | ||ε_base|| / ||ε_total|| | factor_edit metrics w/ and w/o base stream | Position x/y ablation (zero out → measure image change) | GPU-full | base_norm_ratio < 0.5 | base_norm_ratio > 0.9 | Kill criterion: base stream collapses factor streams |
| E6-04 | Per-stream Norm_i + P_i vs shared LN + shared head | H1: Per-stream projection improves editing metrics. H0: No difference. | FGRDiT (per-stream vs shared) | dSprites, 3DShapes | 5 | 1000 | DDIM η=0 | target_accuracy, off_target_mean | leakage_matrix, additive decomposition error | Parameter count must be matched | GPU-full | Per-stream > shared on at least one primary metric | Per-stream ≤ shared | Per-stream projection not worth the parameter cost |
| E6-05 | Gate exposure schedule ablation | H1: Smooth gate schedule best. H0: Constant gate=1 best. | FGRDiT (gate_schedule: constant, linear_decay, cosine, Beta(0.5,0.5)) | dSprites, S0 | 5 | 1000 | DDIM η=0 | target_accuracy, off_target_mean | gate_entropy_over_training, gate_response_curve | Gate=1 (no training) as lower bound | GPU-full | Best schedule > gate=1 on ≥1 metric | All schedules ≤ gate=1 | Gate training not beneficial — drop |
| E6-06 | Graph edge sparsity ablation | H1: Sparse correct graph > dense graph. H0: More edges always better. | FGRDiT (correct, empty, chain, tree, DENSE_DIRECTED) | Causal3DIdent, 3DShapes | 5 | 1000 | DDIM η=0 | leakage_matrix, edge_utilization (attention weight entropy) | target_accuracy, FID | NC4 | GPU-full | Edge utilization entropy > 0.5 (edges not uniform) | Uniform attention weights (entropy → 1.0) | RISK-007: edges never actually used |
| E6-07 | Path Non-Interference Theorem trajectory-level validation | H1: Trajectory max_diff < 1e-3. H0: > 1e-3. | FGRDiT (trained) | dSprites, 3DShapes | 5 | 1000 | DDIM η=0 | max_abs_diff (full trajectory, cut vs changed-factor) | per_timestep error | FACTOR_SOURCE_CUT mode + factor change | GPU-full | max_diff < 1e-3 | max_diff > 1e-2 | Theorem doesn't hold at trajectory level — scope redefinition needed |
| E6-08 | Paired vs unpaired evaluation comparison | H1: Paired gives tighter CIs and higher sensitivity. H0: Paired = unpaired. | FGRDiT | dSprites, S0 | 5 | 1000 | DDIM η=0 (paired) vs independent samples (unpaired) | CI_width_ratio, effect_size_detectable | NC1 (same trace = identical), NC2 (different trace = baseline) | Same model, same conditions, different eval protocols | GPU-full | Paired CI narrower by >20% | Paired CI width ≥ unpaired | RISK-012: paired evaluation shows zero benefit |
| E6-09 | Causal3DIdent neural_graph_surgery ablation grid | H1: neural_graph_surgery gives measurable advantage on non-descendant leakage. H0: No advantage. | FGRDiT (correct graph) | Causal3DIdent, S0 | 5 | 1000 | DDIM η=0 | non_descendant_leakage (surgery vs edit vs node_deletion) | per-node surgery effectiveness | All 8 intervention modes compared | GPU-full | surgery < edit (direction-only) | surgery ≥ edit | Edge-level intervention doesn't add value beyond node-level |

## Success Criteria Summary

| Stage | Success Condition | Gate |
|-------|------------------|------|
| 0 | All 5 tests pass | → Stage 1 |
| 1 | All 5 tests pass | → Stage 2 |
| 2 | E2-01 + E2-02 pass | → Stage 3 |
| 3 | E3-01 + E3-02 pass (no NaN, loss decreasing) | → Stage 4 |
| 4 | E4-01 passes (FGR < DiT on leakage) | → Stage 5 |
| 5 | E5-03 passes (correct graph < wrong graph) | → Stage 6 |
| 6 | E6-01 passes (FGR best on leakage across datasets) | → Paper submission |

**If any gate fails**: halt at that stage, investigate, decide whether to fix, pivot, or abandon.
