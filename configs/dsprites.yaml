dataset: dsprites
image_size: 64
patch_size: 4
in_channels: 1
n_factors: 3
factor_sizes: [3, 6, 40]
factor_names: ["shape", "scale", "orientation"]
trunk_dim: 512
branch_dim: 256
n_trunk_blocks: 12
n_branch_layers: 4
n_heads: 8
graph_type: independent
dag_edges: []
use_base: true
use_gating: true
schedule: cosine
